[Unit]
Description=PrintUpkeep first-boot setup (state directory)
ConditionPathExists=!/var/lib/printupkeep/.firstboot-done
After=local-fs.target
Before=printupkeep-connector.service

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/bin/sh -c 'mkdir -p /var/lib/printupkeep/state && chown -R printupkeep:printupkeep /var/lib/printupkeep && chmod 0750 /var/lib/printupkeep /var/lib/printupkeep/state && touch /var/lib/printupkeep/.firstboot-done'

[Install]
WantedBy=multi-user.target
