# PrintUpkeep OS sshd hardening. SSH is off by default; the connector web UI
# (Remote access switch) or Raspberry Pi Imager turns it on. When it is on:
#   - the login account is "pi"; root has no password and cannot log in
#   - password guessing is throttled
PermitRootLogin no
MaxAuthTries 3
LoginGraceTime 20
